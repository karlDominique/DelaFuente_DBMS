SELECT fname, lname, age, schedule.session, schedule.activity, schedule.date, trainor.name FROM client_member INNER JOIN schedule ON client_member.client_ID = schedule.client_ID INNER JOIN trainor ON trainor.sched_ID = schedule.sched_ID WHERE age < 25;